[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Set these in Netlify UI → Site settings → Environment variables:
# XAI_API_KEY
# ADMIN_PASSWORD
# AUTH_SECRET
# DEFAULT_CREDITS (optional)
