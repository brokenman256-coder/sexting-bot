# Persona images

Upload these JPG files to `public/personas/`:
- nova.jpg
- sienna.jpg
- jade.jpg
- ruby.jpg

They are generated AI portraits for the chat UI (~250KB each).
Binary push via MCP was skipped; add them manually or via `git push` with credentials.
